# Issue templates

Before going public, replace `OWNER/REPO` in `config.yml` with your GitHub
org/repo slug (or remove those `contact_links` entries and rely on SECURITY.md).

Security contact: `xopJack@163.com` — keep in sync with
[SECURITY.md](../SECURITY.md) and the READMEs.
